order of operations:
password change
remove users
cronjobs
system.d


to check for running crontabs:
sudo ls -1 /var/spool/cron/crontabs

to view and edit crontabs:
sudo crontab -u <username> -e

to view aliases:
alias

to remove all aliases for the session:
unalias -a

to edit aliases:
sudo nano ~/.bashrc

to save alias changes:
sudo source ~/.bashrc OR restart...

how to check for odified binaries:
sudo debsums -s

how to fix them:
apt-get install --reinstall $(dpkg -S $(debsums -c) | cut -d : -f 1 | sort -u)

How to get back in with a root shell
1. hard stop the VM
2. Start the VM
3. Spam Escape while booting
4. once menu shows up, press e
5. In following menu, find the line that says "linux". 
In that line, find the ro part and change it to rw. Then, at end of line, add this: 'init=/bin/sh'. Then, ctrl+x to enter shell

Things to look for in pam.d


If package says it doesn't exist

cd /etc/apt
cd /preferences.d <or /preferences>
sudo nano apt
remove all priority -1 commands

languages:
sudo nano /etc/default/locale   

in file edit:
LANG=en_US.UTF-8
LANGUAGE=en_US   

